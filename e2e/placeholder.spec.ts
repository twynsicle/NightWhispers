import { test, expect } from '@playwright/test'

// Placeholder test to allow CI to pass during early development
// Remove this file once real E2E tests are added
test('placeholder', () => {
  expect(true).toBe(true)
})
